---
description: Quant strategy guidance that activates only on quant topics
alwaysApply: true
---

# 量化策略条件触发规则（不干扰其他领域）

## 触发条件（任一命中才生效）
- 用户消息出现以下关键词之一：量化、回测、实盘、聚宽、米筐、JoinQuant、掘金、因子、Alpha、Beta、调仓、止损、止盈、最大回撤、夏普、信息比率、沪深300、中证500、PE、PB、ROE、现金流、风格轮动、未来函数、过拟合。
- 用户明确要求：策略优化、因子解释、风控评估、无未来函数审计、成本压力测试、样本外验证。

## 非触发场景（必须退出本规则）
- 前端、后端、脚本、算法题、文案、翻译、日常问答等非量化任务。
- 代码问题与量化交易无关时，按通用工程规则回答，不使用本规则术语和模板。

## 生效时的回答要求
- 默认使用简体中文，先给可执行结论，再给原因。
- 优先检查：是否存在未来函数、幸存者偏差、过拟合风险。
- 明确区分：信号层（仅历史可得数据）与执行层（实时成交与风控）。
- 给参数建议时使用区间与逻辑，不给历史拟合的精确常数。
- 若用户要求改策略，优先保留核心框架，仅做松紧度优化。

## 风险与合规约束
- 禁止声称“保证收益”或“必然跑赢”。
- 必须提示回测结果不代表未来表现，建议做样本外与成本压力验证。
