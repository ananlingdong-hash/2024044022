## 任务：AI Stock Platform UI 极致美化

### 核心要求
你是世界顶级产品设计师。对这个项目进行**极度高级**的视觉重设计，对标 Bloomberg Terminal、Stripe Dashboard、Linear、Vercel 等顶级产品的设计语言。

### 设计方向

1. **色彩体系升级**
   - 主色调：保留暗色基调，但使用更精致的色彩层次（参考 Linear 的 slate + accent 方案）
   - 渐变：使用 subtle 的微渐变，不要大面积生硬渐变
   - 玻璃态：提升 glassmorphism 效果，更细腻的 backdrop-blur + 边框高光
   - 发光效果：关键元素使用 box-shadow + glow（参考 Stripe 的按钮发光）

2. **字体排版**
   - 引入更优质的字体组合（如 Inter/Geist 作为 body，JetBrains Mono 作为等宽）
   - 层次分明的字号阶梯（text-xs → text-5xl 都要有视觉意义）
   - 字母间距、行高精细化调整
   - 数字/数据使用 tabular-nums 对齐

3. **间距与布局**
   - 使用更精准的 spacing scale（参考 8px grid）
   - 卡片间距、内边距都要经过精心计算
   - 网格布局更细腻的 gap 控制

4. **组件美化**
   - 所有卡片：更精致的 border、shadow、hover 效果、微妙的 gradient 背景
   - 按钮：更大胆的 hover/active 状态，glow 效果
   - 输入框：focus 时的光环动画
   - 导航栏：更精致的 active 指示器、hover 过渡
   - 徽章/标签：更丰富的色彩编码

5. **动效提升**
   - 页面切换使用更流畅的 spring 动画
   - 添加 scroll-triggered 微动效
   - 数字变化时使用 count-up 动画
   - 卡片 hover 时的 3D tilt 微效果
   - Loading skeletons 更精致

6. **图表美化**
   - 所有 ECharts/Lightweight Charts 的配色方案升级
   - 网格线更淡雅
   - tooltip 样式重新设计
   - 图例/标签字体优化

7. **新功能添加（只增不减）**
   - 主题切换：Dark/Light/Auto 三模式，使用 CSS 变量驱动
   - 页面顶部添加面包屑导航
   - Dashboard 顶部添加实时时钟 + 市场状态指示器
   - 全局 Toast 通知系统
   - 数据刷新按钮 + 最后更新时间显示
   - 侧边栏折叠/展开功能
   - 快捷键提示面板（按 ? 键显示）
   - 页面底部的状态栏（连接状态、数据延迟等）

### 重要约束
- **不能删除任何现有功能、页面、API 调用**
- **不能改变路由结构**
- **保持所有组件接口兼容**
- **CSS 变量驱动主题，确保可维护性**
- 新增功能通过 toggle/设置控制，默认开启

### 执行步骤
1. 先读取所有源文件了解现有代码
2. 设计新的 CSS 变量体系（在 index.css 中）
3. 逐步美化每个组件和页面
4. 添加新功能组件
5. 更新 AppShell 布局
6. 确保 npm run build 成功
7. 构建生产版本到 dist/

当你全部完成后，输出 "ALL DONE" 并说明完成了哪些工作。
